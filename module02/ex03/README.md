# Compilation
```
make            		|| shows messages from all methods
make BUILD=ndebug		|| not showing messages from methods
make BUILD=test			|| shows only true test cases
make BUILD=false_cases	        || shows both false and true test cases
```